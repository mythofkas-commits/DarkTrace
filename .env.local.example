# Example only. Do not commit real keys.
#
# Preferred (server-side): set GEMINI_API_KEY in your shell / CI secret store, e.g.:
# PowerShell: $env:GEMINI_API_KEY="..."
# cmd.exe:    set GEMINI_API_KEY=...
#
# If you insist on a local file for development, keep it untracked and ignored.
GEMINI_API_KEY=REPLACE_ME

